name: appback-ai-agent

services:
  agent:
    build: .
    restart: always
    env_file: .env
    volumes:
      - agent_models:/app/models
      - agent_data:/app/data

volumes:
  agent_models:
  agent_data:
